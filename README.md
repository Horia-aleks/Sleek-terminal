# Sleek Terminal

Modern terminal app for macOS built with Electron.

## Setup
```bash
cd sleek-terminal
npm install
npm run build (optional)
npm start
```

## Shortcuts
- `Cmd+T` - New tab
- `Cmd+W` - Close tab
- `Ctrl+U` - Clear line
